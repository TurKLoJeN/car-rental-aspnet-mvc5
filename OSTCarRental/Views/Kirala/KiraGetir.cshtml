@model OSTCarRental.Models.Entity.TBLKIRALA
@{
    ViewBag.Title = "KiraGetir";
    Layout = "~/Views/Shared/PanelLayout.cshtml";
}

@using (Html.BeginForm("KiraGuncelle", "Kirala", FormMethod.Post))
{
    <div style="margin-bottom:15px"></div>
    <div class="form-group">
        <div style="margin-bottom:15px">
            <label>İşlem ID</label>
            @Html.TextBoxFor(k => k.ID, new { @class = "form-control", @readonly = "readonly" })
        </div>
        <div style="margin-top:20px">
            <label>Araba</label>
            @Html.DropDownListFor(k => k.TBLARABA.ID, (List<SelectListItem>)ViewBag.dgr1, new { @class = "form-control" })
        </div>
        <div style="margin-top:20px">
            <label>İsim Soyisim</label>
            @Html.TextBoxFor(k => k.ISIM, new { @class = "form-control"})
        </div>
        <div style="margin-top:20px">
            <label>Mail Adresi</label>
            @Html.TextBoxFor(k => k.MAIL, new { @class = "form-control" })
        </div>
        <div style="margin-top:20px">
            <label>Telefon</label>
            @Html.TextBoxFor(k => k.TELEFON, new { @class = "form-control" })
        </div>
        <div style="margin-top:20px">
            <label>Alış Tarihi</label>
            @Html.TextBoxFor(k => k.ALISTARIH, new { @class = "form-control" })
        </div>
        <div style="margin-top:20px">
            <label>Getiriş Tarihi</label>
            @Html.TextBoxFor(k => k.GETIRTARIH, new { @class = "form-control" })
        </div>

        <div style="margin-top:20px;">
            <button class="btn btn-warning">Kira Güncelle</button>
            <input type="hidden" name="DURUM" value="True" />
        </div>
    </div>
}
